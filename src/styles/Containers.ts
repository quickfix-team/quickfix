import styled from 'styled-components';

export const ContainerBudgetStyles = styled.div`
    width: 18.75rem;
    height: 25rem;
    background-color: var(--color-primary);
`;

export const ContainerRegisterStyles = styled.div`
    width: 22.5rem;
    height: 37.5rem;
    background-color: var(--color-primary);
`;

export const ContainerLoginStyles = styled.div`
    width: 22.5rem;
    height: 25rem;
    background-color: var(--color-primary);
`;

export const ContainerContactStyles = styled.div`
    width: 22.5rem;
    height: 31.25rem;
    background-color: var(--color-primary);
`;

export const ContainerAddressStyles = styled(ContainerContactStyles)`
`;